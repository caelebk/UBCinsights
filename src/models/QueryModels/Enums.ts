export enum MField {
	avg = "avg",
	pass = "pass",
	fail = "fail",
	audit = "audit",
	year = "year"
}

export enum SField {
	dept = "dept",
	id = "id",
	instructor = "instructor",
	title = "title",
	uuid = "uuid"
}

export enum Logic {
	AND = "AND",
	OR = "OR"
}

export enum MComparatorLogic {
	GT = "GT",
	LT = "LT",
	EQ = "EQ"
}

